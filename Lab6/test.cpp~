#include<iostream>
#include<fstream>
#include<string>
using namespace std;
void bubbleSort(int *arr,int n);
int main()
{
  ifstream in("input.txt");
  ofstream out("output.txt");
  int n;
  n<<in;
  int *p=new int[n];
  for(int i=0;i<n;i++)
    {
      p[i]<<in;
    }
  bubbleSort(p,n);
  for(int i=0;i<n;i++)
    {
      out<<p[i]<<endl;
    }
  delete p;
  return 0;
}

void bubbleSort(int *arr, int n)
{
  int min;
  for(int i=0;i<n-1;i++)
   
    {
      for(int j=0;j<n;j++)
	{
	  if(p[i]>p[j])
	    {
	      temp=p[i];
	      p[i]=p[j];
	      p[j]=temp;
	    }
	}
    }
}
