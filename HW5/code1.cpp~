#include<iostream>
#include<fstream>
#include<string>
using namespace std;

int main()
{
  ifstream in("input.txt");
  string s1[10];
  if(!in)
    {
      cout<<"Error: cannot open file!"<<endl;
    }
  else
    {
      for(int i=0;i<10;i++)
	{
	  in>>s1[i];
	  cout<<s1[i];
	}
     
    }
}
